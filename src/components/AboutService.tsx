import { AboutServiceCard, Container } from "./assets";
export interface Services {
  name: string;
  icon: string;
  description: string;
}

export const AboutService = () => {
  return <Container background="bg-white"></Container>;
};
